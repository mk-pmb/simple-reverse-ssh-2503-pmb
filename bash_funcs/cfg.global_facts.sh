# -*- coding: utf-8, tab-width: 2 -*-
#
# These facts are not meant to be actually configurable, but since ghciu
# doesn't have a fact namespace, and having them configurable isn't enough
# of a problem to warrant making a separate namespace, using CFG is
# probably good enough.

CFG[tcp_max_port]=65535

